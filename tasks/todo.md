# v3.0.0 — план мажорного релізу

Мета: закрити всі відомі розриви (протокол MCP, покриття Jira API, надійність), прибрати
надлишковість логіки і не зламати жодної наявної поведінки.

Стан на старті: 54 інструменти, 34 промпти, один файл `src/index.ts` (~4700 рядків),
нуль автотестів у репозиторії.

---

## Результати аудиту (факти, з яких виходить план)

### Дефекти, які треба виправити

| # | Що | Де | Наслідок |
|---|----|----|----------|
| B1 | `jira_bulk_transition_issues` шукає перехід **тільки за `t.name`** | `handleBulkTransitionIssues` | Той самий дефект, що виправили у 2.9 для `jira_update_issue`, лишився в bulk. `transitionName: "In Progress"` не знайде переходу `Start work (estimate)` |
| B2 | `jira_get_issue` **ніколи не повертає `issuelinks`** | `mapIssue` | Промпт `jira-dependency-map` прямо каже "jira_get_issue returns issuelinks". Промпт обіцяє те, чого інструмент не дає. Весь сценарій трасування блокерів не працює |
| B3 | `assignee` має **три різні форми** у відповідях | `search` -> об'єкт, `sprint`/`epic_issues`/`search_by_filter` -> рядок, `get_user_issues` -> відсутній | Модель не може писати єдиний код обробки; доводиться вгадувати форму по інструменту |
| B4 | Немає `startAt` **ніде** | comments, changelog, worklogs, projects, boards, sprints, filters, users | Жорстка стеля 100 записів без способу дійти до наступної сторінки. Задача з 300 коментарями читається лише на третину |
| B5 | Немає обробки 429 / `Retry-After` | всі клієнти | `jira_bulk_create_issues` (до 50) і `jira_bulk_transition_issues` впираються в rate limit і падають без ретраю |

### Надлишковість (те, що треба звести до одного джерела)

| # | Дублювання | Кратність |
|---|-----------|-----------|
| R1 | Хардкод рядка `fields: 'summary,status,...'` | 6 місць, 4 різні набори |
| R2 | Мапінг задачі у списку (key/summary/status/...) | 6 місць, 3 несумісні форми |
| R3 | Побудова JQL `project = "X" AND ...` + екранування лапок | 2 місця |
| R4 | Патерн `if (x !== undefined && x !== null)` | ~40 місць |
| R5 | `validateMaxResults` + збирання `params` | ~12 місць |
| R6 | Форма пагінації у відповіді (`count` vs `total`, `isLast`, `nextPageToken`) | 8 місць, 2 несумісні форми |
| R7 | Інтерфейси, оголошені всередині функцій (`AgileEpic`, `WatcherUser`, `JiraFilter`, `TR`) | 4 місця |
| R8 | Мапінг вкладень / фільтрів | по 2 місця |
| R9 | Реєстрація інструмента у 3 незалежних місцях (схема, хендлер, `toolHandlers`) | 54 рази |
| R10 | `handleGetIssueTypes` і `handleGetFields` б'ють в API повз кеш метаданих, доданий у 2.9 | 2 місця |
| R11 | Пошук переходу за назвою | 2 місця, різна логіка (див. B1) |

### Розриви протоколу MCP

- `capabilities` містить лише `tools` і `prompts`
- нуль анотацій інструментів (`readOnlyHint` / `destructiveHint` / `idempotentHint`)
- нуль `outputSchema` / `structuredContent` — усе повертається як `JSON.stringify` у текстовому блоці
- промпти без аргументів: `ListPrompts` віддає лише `name` + `description`
- немає `completion/complete`
- немає ресурсів

### Розриви покриття Jira

Спринти (create/update/state), версії (create/update/release/delete), компоненти (CRUD),
`editmeta` як інструмент, масове редагування полів, віддалені лінки, видалення вкладення,
видалення лінка, `mypermissions`, статуси проєкту, список міток, ранжування, беклог,
`fields`/`expand` у пошукових інструментах, видимість коментарів (internal/JSM).

---

## Інваріанти: що НЕ можна зламати

Ці пункти перевіряються тестом на кожній фазі. Регресія будь-якого = стоп.

- [ ] `validateAttachmentPath` обмежує шлях до cwd + `$HOME` (виправлено у 2.3.8)
- [ ] `handleGetUserIssues` квотує `projectKey` у JQL і проганяє `accountId` через `validateAccountId`
- [ ] `handleAssignIssue` / `handleAddWatcher` / `handleRemoveWatcher` проганяють `accountId` через `validateAccountId`
- [ ] `handleAddWorklog` проганяє `started` через `validateISO8601`
- [ ] Параметри, що йдуть у URL (`commentId`, `linkType`, `issueType`, `priority`, `worklogId`, `filterId`, `attachmentId`, `transitionId`), проходять `validateSafeParam` (блокує `/` і `\`)
- [ ] `JIRA_HOST` мусить починатися з `https://`
- [ ] `handleError` не витікає токен і не віддає конфіг запиту
- [ ] Жодного запису в stdout, окрім JSON-RPC (інакше ламається stdio-транспорт)
- [ ] Сервер не читає `.env` з файлової системи (Socket-аудит)
- [ ] Рантайм-залежності пінуються точними версіями
- [ ] Мультипарт-завантаження шле `X-Atlassian-Token: no-check`
- [ ] `validateCustomFields` не дозволяє перезаписати системні поля через `customFields`

---

## Рішення, які треба ухвалити до старту

### D1. Розбивати `src/index.ts` на модулі?

`CLAUDE.md` фіксує однофайлову архітектуру як свідому ("simplifies build, no module
resolution surprises for npx, easier to audit"). Після цього релізу файл буде ~7000 рядків.

- **Рекомендація: розбити**, помірно — 10-12 файлів за доменом, без мікромодулів.
  `tsc` компілює в `dist/` зі звичайними відносними ESM-імпортами, для `npx` нічого не
  змінюється; аудит стає легшим, а не важчим, бо безпекові інваріанти збираються в один
  `validation.ts` замість розсіювання по 4700 рядках.
- Альтернатива: лишити один файл із секціями-регіонами. Тоді фази 1 і 4 роблять те саме,
  але навігація по файлу лишається важкою.
- **Це рішення ваше — воно переписує задокументовану конвенцію проєкту.**

### D2. Що робити з надлишковими інструментами?

`jira_list_epics`, `jira_get_user_issues`, `jira_search_by_filter`, `jira_get_board_epics`,
`jira_get_attachments` — це `jira_search_issues` / `jira_get_issue` з фіксованим JQL або
набором полів.

- **Рекомендація: залишити всі імена, але перевести на спільне ядро** (тонкі обгортки над
  одним пошуковим движком) і позначити в описі канонічний інструмент. Прибирає надлишковість
  *логіки*, не ламаючи жодного наявного сценарію і жодного промпта.
- Альтернатива: видалити 5 інструментів у мажорі. Чистіша поверхня, але ламає промпти
  користувачів і будь-яку їхню автоматизацію.

### D3. Примусова англійська від Jira

`Accept-Language: en-US` + `X-Force-Accept-Language: true` (перевірити на вашому інстансі
одним curl) змусили б Jira віддавати канонічні назви і зняли б проблему локалізації в корені.

- **Рекомендація:** env `JIRA_FORCE_ENGLISH` (типово `false`), бо вмикання змінює те, що
  користувач бачить у відповідях. Резолюція name->id лишається як була і працює незалежно.

### D4. Обсяг ламких змін

Мажор дозволяє ламати. Пропоную рівно три ламкі зміни, кожна — усунення несумісності:

1. `assignee` у всіх списках стає об'єктом `{accountId, displayName}` (B3)
2. Пагінація уніфікується: `{total, isLast, nextPageToken, startAt}`; поле `count` зникає (R6)
3. Форма елемента списку задач стає однаковою для всіх інструментів (R2)

Все інше — адитивне. Перейменувань інструментів немає, видалень немає.

---

## Фаза 0 — Мережа безпеки — ГОТОВО

У репозиторії нуль тестів. Вимога "не зламай" при рефакторингу такого обсягу без
регресійного набору нездійсненна. Спершу фіксуємо поточну поведінку, потім міняємо.

- [x] Мок-інстанс Jira і stdio-клієнт у `test/` (`mock-jira.mjs`, `client.mjs`, `tls.mjs`)
- [x] Мок покриває всі 51 ендпоінт, які сервер реально смикає (`/rest/api/3` + `/rest/agile/1.0`)
- [x] Контрактні знімки: 63 сценарії на всі 54 інструменти — фіксують і відповідь, і трасу HTTP-запитів
- [x] Тест, що жоден зареєстрований інструмент не лишився без контрактного сценарію
- [x] Знімки `tools/list`, `prompts/list`, `initialize` — ловлять зміну схем і capabilities
- [x] `security.test.mjs` — 12 інваріантів безпеки, кожен окремим тестом
- [x] `defects.test.mjs` — B1-B5 як `todo`-тести: документують дефект, не валять збірку
- [x] `npm test` без мережі і без облікових даних (мок на ефемерному порту, самопідписаний cert)
- [x] CI: `test.yml` на push/PR (Node 20, 22 + build на 18); тести як гейт у `publish.yml`
- [x] Базова лінія: 92 тести, 84 pass, 0 fail, 8 todo — детерміновано на повторних прогонах

Ключова деталь: хост мока нормалізується у знімках (`https://jira.test`), бо порт ефемерний.

Критерій виходу досягнуто: будь-яка зміна поведінки тепер видима в дифі знімків.

---

## Фаза 1 — Внутрішня архітектура, нуль змін поведінки

Усі знімки Фази 0 мусять лишитися байт-у-байт ідентичними, крім свідомих ламких змін D4,
які вносяться окремим комітом з оновленням знімків.

### 1.0 Розбиття на модулі (D1) — ГОТОВО

- [x] 26 модулів: `config`, `types`, `validation`, `adf`, `http`, `errors`, `responses`, `meta`,
      `transitions`, `mappers`, `schemas`, `prompts`, `index` + `tools/` за доменами
- [x] Витяг механічний, скриптом — усі 84 знімки байт-у-байт ідентичні після розбиття
- [x] Валідатори безпеки зібрані в один `validation.ts`
- [x] Мертві імпорти вичищені (тип-імпорти `--noUnusedLocals` не ловить, прибрано окремо)

Пастка Node16: відносні імпорти пишуться з `.js`, хоча джерела `.ts`.

### 1.1 Реєстр інструментів (закриває R9, вмикає Фазу 4) — ГОТОВО

- [x] `defineTool({ name, description, annotations, inputSchema, handler })`, `tools/definitions.ts` видалено
- [x] `tools/list` і диспетчер `tools/call` виводяться з одного масиву
- [x] `handlerMap` кидає на дубльоване ім'я інструмента
- [x] Знімок `tools/list` не змінився — доказ, що перенос точний
- [x] Тест на кількість інструментів, тип схеми і непорожній опис

### 1.2 Транспортний шар (готує Фазу 2) — ГОТОВО

- [x] `createClient()` — одна точка створення обох клієнтів; `clients[]` для навішування ретраю
- [x] `JIRA_TIMEOUT_MS`, `JIRA_MAX_RETRIES`, `JIRA_FORCE_ENGLISH`
- [x] Відхилення від плану: замість переписування ~100 місць виклику на обгортки
      `get/post/put/del` — перехоплювачі axios. Та сама єдина точка для ретраїв і
      заголовків, без churn у кожному хендлері. Підміна транспорту в тестах уже
      вирішена HTTPS-моком, тож інтерфейс `JiraClient` не потрібен.

### 1.3 Мапери (закриває R1, R2, B3, B4, R6) — ГОТОВО

- [x] `ISSUE_LIST_FIELDS` — одна константа замість 6 хардкодів у 4 варіантах
- [x] `mapIssueSummary()` для списків, `mapIssue()` для повної задачі, `mapUser()` для людей
- [x] Єдиний конверт пагінації `{returned, hasMore}` + `{startAt, total}` або `{nextPageToken}`
- [x] `startAt` у 11 інструментах
- [ ] `mapAttachment`, `mapWorklog`, `mapComment`, `mapFilter` — лишились дублі по 2 місця (R8)
- [ ] Локальні інтерфейси з тіл функцій переїжджають у типи (R7)

### 1.4 Хелпери аргументів (закриває R4, R5) — ГОТОВО

- [x] `present()`, `readMaxResults()`, `readStartAt()`, `readPageToken()`, `optionalText()`
- [x] `offsetParams` / `offsetPage` / `tokenPage` — один розбір і один конверт пагінації
- [x] `numericId` для `boardId` / `sprintId` замість трьох ad-hoc перевірок

### 1.5 JQL (закриває R3) — ГОТОВО

- [x] `buildJql()` / `equalsClause()` / `quoteJqlValue()` — єдине місце екранування
- [x] Екранується і зворотний слеш, не лише лапка: раніше кінцевий `\` міг екранувати
      закривальну лапку і вийти зі стрічки. Покрито тестом.

### 1.6 Переходи (закриває B1, R11) — ГОТОВО

- [x] Один `resolveTransition(list, { id, status })`: спершу `to.name`, потім `name`, без регістру
- [x] `jira_update_issue` і `jira_bulk_transition_issues` користуються ним обидва
- [x] `jira_bulk_transition_issues` отримав `transitionFields` і йде через `postTransition`
- [x] `describeTransitions` — один формат "name -> target (id)" для обох
- [x] `transitionName` лишається аліасом до нового `status`

### 1.7 Кеш метаданих (закриває R10) — ГОТОВО

- [x] `handleGetIssueTypes` і `handleGetFields` читають через `fetchIssueTypes` / `fetchFieldIndex`
- [x] Побічно знято мовчазну стелю в 50 типів задач

### 1.8 Розділення відповідальностей у хендлерах (SRP)

- [ ] Кожен хендлер: розбір аргументів -> побудова payload -> виклик клієнта -> мапінг. Без змішування
- [ ] Побудова payload для create/update вже винесена (`applyOptionalFields`) — довести до кінця

Критерій виходу: `npm test` зелений, дифи знімків або порожні, або збігаються з D4.

---

## Фаза 2 — Надійність (закриває B4, B5) — ГОТОВО

- [x] 429 + `Retry-After`: експоненційний backoff з джитером, стеля спроб, стеля затримки 30 с
- [x] Політика ретраїв: 429 — і читання, і записи (rate limit означає, що запит не виконався);
      5xx і мережеві помилки — **тільки читання**; решта 4xx — ніколи
- [x] `Retry-After` довший за 60 с не поважається: краще віддати помилку, ніж підвісити агента
- [x] Обмежувач паралельності `mapWithConcurrency` для bulk-переходів, результати в порядку входу
- [x] `startAt` у списках (зроблено у Фазі 1.3)
- [x] Уніфікована форма пагінації + `hasMore` (зроблено у Фазі 1.3)
- [x] `JIRA_TIMEOUT_MS`, `JIRA_MAX_RETRIES`, `JIRA_RETRY_BASE_MS`, `JIRA_CONCURRENCY`
- [x] `JIRA_FORCE_ENGLISH` (D3)
- [x] `test/retry.test.mjs` — 9 тестів: ретрай читання і запису на 429, стеля спроб,
      довгий `Retry-After`, 5xx на читанні, **відсутність** ретраю на create/delete при 5xx,
      відсутність ретраю на 4xx, порядок результатів bulk під паралельністю

Свідомо не паралелив цикл метаданих у `jira_bulk_create_issues`: там усе йде з кешу після
першої задачі, паралельність дала б N промахів кешу замість одного запиту.

---

## Фаза 3 — Покриття Jira API — ГОТОВО

75 інструментів. Кожен новий має схему поряд із хендлером, маршрут у моці,
контрактний знімок і поведінковий тест.

### 3.1 Спринти
- [x] `jira_create_sprint`, `jira_update_sprint` (назва/ціль/дати/стан одним інструментом), `jira_delete_sprint`

### 3.2 Версії
- [x] `jira_create_version` (резолвить ключ проєкту в числовий id, бо ендпоінт ключі не приймає)
- [x] `jira_update_version` — реліз це `released: true`, окремого інструмента немає
- [x] `jira_delete_version` через `removeAndSwap`, а не застарілий `DELETE`

### 3.3 Компоненти
- [x] `jira_create_component`, `jira_update_component`, `jira_delete_component` з `moveIssuesTo`

### 3.4 Задачі
- [x] `jira_get_edit_fields` — дзеркало `jira_get_create_fields` для update
- [x] `jira_bulk_update_issues` з `addLabels`/`removeLabels` через verb `update`
- [x] `issuelinks` у `mapIssue` (**B2**) + `jira_delete_issue_link`
- [x] `jira_get_remote_links` / `jira_add_remote_link` / `jira_delete_remote_link`, лише http і https
- [x] `jira_delete_attachment`
- [x] `visibility` і `internal` (JSM) у коментарях

### 3.5 Метадані і права
- [x] `jira_get_project_statuses`, `jira_list_labels`, `jira_get_my_permissions`

### 3.6 Дошки
- [x] `jira_rank_issues`, `jira_move_to_backlog`

### 3.7 Пошук
- [x] `fields` / `expand` / `includeCustomFields` у шести пошукових інструментах через спільний хелпер

Промпти `jira-sprint-planning` і `jira-version-planning` більше не відсилають у Jira UI.

## Фаза 4 — Протокол MCP — ГОТОВО

### 4.1 Анотації
- [x] Усі 75 інструментів: `readOnlyHint` (36), `destructiveHint` (10), `idempotentHint` (61), `openWorldHint`
- [x] Тест звіряє `readOnlyHint` із HTTP-методами у записаних трасах — анотація стала фактом, а не обіцянкою
- [x] Виняток `jira_download_attachment` (пише файл на диск) задокументований у тесті з причиною

### 4.2 Структурований вивід
- [x] `structuredContent` на кожній успішній відповіді, текстовий блок збережено для старих клієнтів
- [x] `outputSchema` на 44 інструментах із 13 спільних схем
- [x] Схеми навмисно м'які (`additionalProperties`, мінімальний `required`)
- [x] Тест валідує кожен `outputSchema` проти всіх записаних відповідей; міні-валідатор у `test/`, нуль рантайм-залежностей

### 4.3 Промпти з аргументами
- [x] 21 промпт оголошує `arguments`; `<KEY>` підставляється реальним ключем проєкту
- [x] Відсутній обов'язковий аргумент — явна помилка, а не мовчазний зламаний промпт
- [x] `completion/complete` для `projectKey`, `issueType`, `priority`, `state` з живого інстансу

### 4.4 Ресурси
- [x] Шаблони: задача, проєкт, екран створення, фільтр; статичні: проєкт за замовчуванням і `jira://my-open-issues`
- [x] URI валідуються тими самими валідаторами, що й аргументи інструментів
- [x] Без підписок — не той профіль навантаження

### 4.5 Решта
- [x] `capabilities` оголошує `tools`, `prompts`, `resources`, `completions`
- [ ] `logging` не оголошуємо: не реалізовано `logging/setLevel`, а оголошувати нереалізоване гірше, ніж не оголошувати

## Фаза 5 — Ламкі зміни і консолідація — ГОТОВО

- [x] Ламкі зміни внесені разом із відповідними рефакторами, кожна з переглядом дифу знімків
- [x] `runJqlSearch` / `runAgileIssueList` — надлишкові інструменти стали тонкими обгортками
      над спільним ядром; жодне ім'я не змінилось, жоден знімок не зрушив
- [x] `MIGRATION.md` — кожна зовні видима зміна з прикладами до/після і чеклістом
- [x] Промпти перевірені: тест довів, що твердження README про згадку кожного інструмента
      було хибним для 19 інструментів (один — ще з 2.8.0). Виправлено, тепер це під тестом.
- [x] Три промпти вчили пагінації 2.x (`isLast`, `count`) — виправлено
- [x] Доданий промпт `jira-project-admin` — це була справжня прогалина, а не місце для
      паркування осиротілих імен

## Фаза 6 — Реліз — ПІДГОТОВЛЕНО, публікація чекає OK

- [x] README: 75 інструментів, розділ про протокол MCP, ліміти й ретраї, ключі env
- [x] CHANGELOG: секція 3.0.0 з підрозділом Breaking; 2.9.0 позначено як неопублікований і
      складений у 3.0.0
- [x] CLAUDE.md: нова архітектура (28 модулів), розділ про тести, оновлені інваріанти безпеки
      і landmines
- [x] `MIGRATION.md`
- [x] `npm pack --dry-run` — у тарбол не потрапляють `src/`, `test/`, `tasks/`
- [x] Димовий запуск через `index.js` (той самий шлях, що й `npx`): сервер стартує, оголошує
      tools, prompts, resources, completions
- [ ] Версія в трьох місцях: `package.json`, `SERVER_VERSION`, заголовок README
- [ ] Ручний димовий тест на реальному інстансі Jira
- [ ] Тег `v3.0.0` -> GitHub Actions -> публікація з provenance

## Порядок і ризики

Фази 0 -> 1 -> 2 -> 3 -> 4 -> 5 -> 6, строго послідовно.

- Фаза 0 без неї решта плану — робота наосліп. Найвища цінність на витрачений час.
- Фаза 1 найризикованіша: багато механічних правок. Мітигація — знімки з Фази 0 і
  окремі коміти на кожен підпункт.
- Фаза 3 найоб'ємніша, але найбезпечніша: тільки додавання.
- Фаза 4.2 (`outputSchema`) друга за ризиком: якщо `structuredContent` розійдеться зі схемою,
  суворі клієнти впадуть. Мітигація — валідація у тестах.

Проміжні релізи не публікуємо. Одна гілка `v3`, злиття в `main` перед тегом.

---

## Огляд результатів

(заповнюється після виконання)
